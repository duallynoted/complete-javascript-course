data encapuslation: allows us to hide the implentation details from the outside scope
so that way, we only expose a public interface, with is sometimes called API
module pattern: the secret of the module pattern is that it returns an object containing of all of the functions we want to be public
separation of concerns: each thing in an appplication should only be concerned in doing one thing independenty
    they dont have to know that they other exists

